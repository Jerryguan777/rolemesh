"""Shared fixtures for evaluation tests.

Empty for now — ``skills`` / ``skill_files`` are part of the standard
schema since ``feat/skills`` merged, so ``test_db`` is sufficient.
Kept around as a hook for future eval-specific fixtures.
"""
